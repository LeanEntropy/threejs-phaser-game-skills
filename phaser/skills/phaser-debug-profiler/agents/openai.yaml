interface:
  display_name: "Phaser Debug Profiler"
  short_description: "Debug and profile Phaser 3 (2D) games"
  default_prompt: "Find and fix this Phaser 3 render/runtime/performance issue with browser evidence and before/after metrics. First load the debug/profile checklists reference, report the reference ledger, and include checklist items used plus baseline/post evidence (actualFps, draw calls, active game objects, physics bodies, particle/tween counts, texture memory)."
